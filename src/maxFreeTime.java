/*public class maxFreeTime {
    public int freeTime(int eventTime, int[] startTime, int[] endTime){

        int indice = 0;
        int contadorHorasLibres = 0;
        for(int i = eventTime; i <= 0; i--){
            for(int j = 0; j <= eventTime; j++) {
                int horas = endTime[j] - startTime[j];
                if(j != eventTime && ){}// quiero comprobarsi la hora actual esta libre(el numero (i o j) + 1)
                if(i < eventTime && contadorHorasLibres == horas){

                }
                if (i + horas <= eventTime &&){}
            }
        }

        return startTime[0];
    }
}
*/