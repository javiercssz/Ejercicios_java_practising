import java.util.*;
public class ejercicio3_examen {
public static void main(String[] args) {
   /** double a; //valor 1
    double b; //valor 2
    double resultado;
    Scanner sc = new Scanner(System.in);
    System.out.println("Bienvenido a la calculadora escriba el primer numero");
    a = sc.nextDouble();
    System.out.println("escriba el operando + - * /");
    String op = sc.next();
    System.out.println("escriba el segundo numero");
    b = sc.nextDouble();
    switch(op)
    {
        case "+" ->
        {
            resultado = a + b;
            System.out.println("el resultado es " + resultado);
        }
        case "-" ->
        {
            resultado = a - b;
            System.out.println("el resultado es " + resultado);
        }
        case "*" ->
        {
            resultado = a * b;
            System.out.println("el resultado es " + resultado);
        }
        case "/" ->
        {
            resultado = a / b;
            System.out.println("el resultado es " + resultado);
        }


    }
}
}
/**Ejercicio 3: Menú con la estructura switch (2 puntos)

 Enunciado: Escribe un programa en Java que implemente un menú simple para realizar
 operaciones aritméticas básicas (suma, resta, multiplicación y división) entre dos números.
 El programa debe mostrar un menú con opciones, solicitar al usuario dos números y realizar
 la operación seleccionada utilizando la estructura switch.**/
   int a = 5;
   int b = 7;
   boolean resultado = (a == b);
    System.out.println(resultado);
}}
